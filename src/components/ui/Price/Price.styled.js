import styled from "styled-components";

export default styled.div`
  display: flex;
  gap: 0.5rem;
  margin-bottom: 0.5rem;
  font-size: 0.9rem;
  font-weight: bold;
  div {
    display: flex;
    gap: 1rem;
  }
  strike {
    opacity: 0.5;
    text-align: center;
    margin: auto 0;
  }
`;
