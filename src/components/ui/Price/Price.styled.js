import styled from "styled-components";

export default styled.div`
  display: flex;
  gap: 0.5rem;
  margin-bottom: 0.5rem;
  div {
    display: flex;
    gap: 1rem;
  }
  strike {
    opacity: 0.7;
    text-align: center;
    margin: auto 0;
  }
`;
